# build_alias_index (deprecated)

Alias infrastructure has been removed from this repo.

FrameURL is the only normative identifier.

This tool is retained only as a stub to fail fast if something still tries to invoke it.
